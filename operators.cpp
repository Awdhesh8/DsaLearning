// OPERATORS 


#include <iostream>
using namespace std;

int main (){

    /* 
    int a = 10, b = 5;
    int sum = a + b;

    cout << "Sum = " << (a+b) << endl;
    cout << "Difference = " << (a-b) << endl;
    cout << "Product = " << (a*b) << endl;
    cout << "Division = " << (a/b) << endl;
    cout << "Modulo = " << (a%b) << endl;  // Remainder 
   // cout << "Modulo = " << (a%b) << endl;  // Remainder 
   */

    // Division 

    int a = 5;
    int b = 2;

    int c = 5;
    float d = 2;
    
    int e = 5;
    double f = 3;

    cout << (a / b ) << endl;

     // Division with decimal values
     // cout << "With Decimal value , " << (c / d ) << endl;
    cout << "With Decimal value , " << (e / f ) << endl;

    return 0;

}

